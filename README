Installation:

FeOSMusic requires the soundStream lib for FeOS. You can find it here:

https://github.com/Tomwi/soundStream

steps:
1) place soundStream in $(FEOSSDK)/userlib/
2) Compile soundStream ('make install')
3) Compile FeOSMusic   ('make install')